<%= simple_form_for Favorite.new, url: favorites_destroy_path(id: current_user.favorites.find_by(list_id: @list.id)),
             html: { method: :delete },
             remote: true do |f| %>
  <%= f.button :submit, "Unfollow List", id:"disfavor-btn" %>
<% end %>
